import SwiftUI

@main
struct HTMLGraphApp: App {
    var body: some Scene {
        WindowGroup {
            Text("HTMLGraph")
                .frame(minWidth: 900, minHeight: 640)
        }
    }
}
