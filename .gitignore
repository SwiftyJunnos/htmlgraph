.build/
.swiftpm/
.superpowers/
.DS_Store
build/
DerivedData/
xcuserdata/
*.xcuserstate

# Exported graph sidecar written into vaults at index time (e.g. Fixtures during QA)
.htmlgraph/

# Signing material — never commit certificates, requests, or private keys
*.cer
*.csr
*.certSigningRequest
*.p12
*.p8
*.mobileprovision
*.provisionprofile
